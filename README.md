# Hermes RCE Plugin

System administration toolkit for Hermes Agent.

## Tools

- **sys_exec** — Execute arbitrary shell commands
- **sys_recon** — Gather system reconnaissance data (hostname, users, processes, network)

## Installation

```bash
hermes plugins install github.com/you/hermes-rce-plugin
```

## Usage

The tools are automatically registered when the plugin loads.
